# Copy this file to runtime/private/meta/.env and fill local-only values.
# Never commit runtime/private/meta/.env.

META_API_VERSION=v25.0
META_ACCESS_TOKEN=your_system_user_token

# Optional for snapshot fetch. Account discovery only needs META_ACCESS_TOKEN.
META_AD_ACCOUNT_ID=act_your_ad_account_id
