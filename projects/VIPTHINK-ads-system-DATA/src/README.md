# src

后续代码从最小闭环开始添加：

- Meta read-only adapter
- 诊断规则
- 本地草稿
- 写操作阻断
- 本地报表
- 本地 UI

当前已实现：

- `meta_ads_closed_loop.adapters.meta_readonly`
- `meta_ads_closed_loop.domain`
- `meta_ads_closed_loop.app.local_ui`

当前阶段不迁移旧项目代码。
