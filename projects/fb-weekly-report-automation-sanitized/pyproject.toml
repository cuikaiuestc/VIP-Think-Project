[project]
name = "fb-weekly-report-automation-sanitized"
version = "0.1.0"
description = "Sanitized internal collaboration package for FB weekly report automation."
requires-python = ">=3.10"
dependencies = [
  "openpyxl>=3.1.0",
]

[project.scripts]
fb-weekly-report = "scripts.generate_fb_weekly_report:main"

